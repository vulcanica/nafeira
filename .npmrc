# Allow `npx create-vercel-shop@latest` (which uses npm) to scaffold cleanly
# despite peer-dep conflicts in this bleeding-edge stack. pnpm, bun, and yarn
# ignore this flag and apply their own peer-dep resolution.
legacy-peer-deps=true
